// import { put, select, takeLatest } from 'redux-saga/effects';
// import { actions } from './slice';

// export function* onConnectSaga() {}

export function* walletProviderSaga() {
  // yield takeLatest(actions.disconnect.type, onDisconnectSaga);
}
